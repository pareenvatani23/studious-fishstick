/**
 * Higgsfield-generated looping media, bundled and shipped over-the-air.
 * Components fall back to the SVG mark / animated gradient if these are removed.
 */
import bg from './bg-loop.mp4';
import logo from './logo-loop.mp4';

export const backgroundVideo: number | null = bg;
export const logoVideo: number | null = logo;

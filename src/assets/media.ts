/**
 * Higgsfield-generated looping media. Left null until the assets are generated;
 * components fall back to the SVG mark / animated gradient when null, so the app
 * works with or without them. To enable, drop the mp4s in this folder and set:
 *   export const backgroundVideo = require('./bg-loop.mp4');
 *   export const logoVideo = require('./logo-loop.mp4');
 * (Shipped over-the-air — no native rebuild needed.)
 */
export const backgroundVideo: number | null = null;
export const logoVideo: number | null = null;
